# *.xlsx       — .xlsx is tracked (results files) but lock files are not
~$*.xlsx
~$*.xls
*.xls
.env
.env.*
!.env.example

# Python virtual environments
.venv/
venv/

# Python caches
__pycache__/
*.py[cod]
**/papers_from_database.py

# Claude Code per-machine state (tool-permission cache, not project work)
.claude/settings.local.json

# Large local data folder (Semantic Scholar dumps, ~70 MB) — never commit it.
# (The Beall's List check moved to its own repo: AminDaryan/Beall-s-List-check.)
docs/Updated Abstract Papers/
